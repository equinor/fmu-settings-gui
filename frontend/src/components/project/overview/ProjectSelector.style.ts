import { Dialog } from "@equinor/eds-core-react";
import styled from "styled-components";

export const ExpansiveDialog = styled(Dialog)`
  width: 100%;
`;

export const ProjectSelectorFormContainer = styled.div`
  min-width: 40em;
`;
